package com.example.playlistmaker.common.presentation.model

enum class SharingObjects(var intent: Any? = null) {
    DEFAULT(null),
    TERMS(null),
    SUPPORT(null),
    SHARE_APP(null),
    SHARE_PLAYLIST(null);
}
